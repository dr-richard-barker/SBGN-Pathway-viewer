# Archived panel figures (v1)

The original static panel figures — a clean Python/matplotlib node-link diagram
(from KGML topology) beside a significant-loci heatmap. Kept as a backup while the
richer ggkegg (R) renderings are trialled. Not published in the atlas.

Generator: `build_panels_v1_python.py` (== tools/build_panels.py at archival time).
